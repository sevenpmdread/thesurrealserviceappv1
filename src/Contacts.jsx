import React from "react";
import Typography from "@mui/material/Typography";
const Contacts = () => {
  return (
    <div>
      <Typography variant="h6">This is the Boards tab</Typography>
    </div>
  );
};

export default Contacts;
